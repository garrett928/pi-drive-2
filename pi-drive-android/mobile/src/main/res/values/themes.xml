<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pidrive" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Pidrive" parent="Base.Theme.Pidrive" />

    <!--
      Splash screen theme applied to MainActivity via android:theme in the manifest.
      The SplashScreen API reads these attributes on Android 12+.
      postSplashScreenTheme transitions to Theme.Pidrive after splash exits.
    -->
    <style name="Theme.PiDrive.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/pd_bg_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/pd_bg_dark</item>
        <item name="postSplashScreenTheme">@style/Theme.Pidrive</item>
    </style>
</resources>