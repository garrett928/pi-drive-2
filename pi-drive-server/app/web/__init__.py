"""
Web UI package (REQUIREMENTS.md §9): server-rendered Flask/Jinja pages.

`routes.py` holds the `web` blueprint (pages); `auth.py` holds the API-key
session login and CSRF protection. Templates live in `app/templates/`, the
stylesheet in `app/static/app.css`.
"""
